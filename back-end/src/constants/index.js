const AUTH_PROVIDERS = {
    GOOGLE: "google",
    PASSWORD_LESS: "password_less",
};

const TOKEN_TYPES = {
    ACCESS: "access",
    REFRESH: "refresh",
};

module.exports = { AUTH_PROVIDERS, TOKEN_TYPES };
